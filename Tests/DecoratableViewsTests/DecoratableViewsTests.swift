import XCTest
@testable import DecoratableViews

final class DecoratableViewsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DecoratableViews().text, "Hello, World!")
    }
}
